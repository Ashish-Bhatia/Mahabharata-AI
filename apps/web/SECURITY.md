# Web security baseline

- Next.js is pinned to a patched 15.5.x release.
- Dependency changes require CI before merge.
- Production builds must pass before deployment.
- GitHub Pages deployment runs only from `main`.
